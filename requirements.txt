streamlit>=1.40.0
chromadb>=0.5.5,<2.0.0
fastembed>=0.4.2
rank-bm25>=0.2.2
numpy>=1.26.0

# Streamlit Community Cloud ships an old system sqlite3; Chroma needs >= 3.35.
# The shim in rag_core.py swaps this in. Harmless/skipped on Windows & macOS.
pysqlite3-binary>=0.5.2; sys_platform == "linux"
